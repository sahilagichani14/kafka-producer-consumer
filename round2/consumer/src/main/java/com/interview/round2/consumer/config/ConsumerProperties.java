package com.interview.round2.consumer.config;

import java.util.UUID;

public class ConsumerProperties {
    public static final String GROUP_ID = "email-group"; // should match with consumer app.properties file
    public static final String GROUP_ID_2 = "email-group-2"; // should match with consumer app.properties file
    public static final String GROUP_ID_3 = "email-group-3"; // should match with consumer app.properties file
    public static final String EMAIL_TOPIC_NAME = "email";
}
